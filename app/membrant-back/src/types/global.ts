export enum DailyItemType {
  TASK = 'task',
  GOAL = 'goal',
}

export enum CalendarItemType {
  EVENT = 'event',
  TASK = 'task',
}

export enum TaskStatus {
  TODO = 'todo',
  IN_PROGRESS = 'in progress',
  COMPLETE = 'complete',
}